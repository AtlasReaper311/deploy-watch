{
  "name": "deploy-watch",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Polls Cloudflare Pages' own deployment status and reports genuine outcome changes into Discord.",
  "scripts": { "dev": "wrangler dev", "deploy": "wrangler deploy", "tail": "wrangler tail" },
  "devDependencies": { "wrangler": "^4.0.0" }
}
